// src/init.server.ts - Server entry point
import { KnitServer } from "@rbxts/knit";

KnitServer.Start({
    Services: {
        // Add services here
    },
    Components: {
        // Add components here
    },
});
